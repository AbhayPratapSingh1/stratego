const waitingPage = () => {
  background(1)
  translate(width / 2, height / 2);
  textAlign(CENTER, CENTER);
  textSize(30);
  fill(255);
  stroke(255);
  text("Waiting for other player...", 0, 0)
}

